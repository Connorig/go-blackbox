# go-blackbox 开发规范

本文件适用于整个仓库。每次分析、修改、重构和测试都必须先阅读并遵守以下标准。

## 1. 可读性与可维护性

- 代码首先服务于维护人员阅读，其次才是减少行数。
- 方法保持单一职责；出现多层嵌套、重复分支或过长方法时，应提取语义明确的辅助方法。
- 命名必须表达业务含义，禁止使用无法说明用途的缩写、临时名称或含糊变量名。
- 优先使用清晰直接的实现，禁止为了“炫技”引入不必要的抽象、反射或复杂并发结构。
- 修改公开 API 时优先保持兼容；无法兼容时必须提供迁移说明。

## 2. 注释要求

- 所有新增或修改的类型、接口、常量、变量、函数和方法都必须有说明性注释。
- 导出标识符的注释以标识符名称开头，符合 Go Doc 规范。
- 未导出的关键辅助方法也必须注释，说明其职责和存在原因。
- 注释重点说明：用途、输入约束、调用时机、并发行为、失败行为、返回值含义和资源释放责任。
- 禁止只把代码翻译成中文；能够直接从代码看出的内容不需要重复描述。
- 修改实现后必须同步检查注释，禁止保留与实际行为不一致的旧注释和无效 TODO。

## 3. 错误与日志

- 所有 `error` 必须处理，禁止使用空白标识符静默丢弃错误。
- 当前层能够恢复时，应记录包含功能点和必要上下文的日志；需要上抛时使用 `%w` 保留错误链。
- 初始化失败不得继续注册 nil、不可用或半初始化实例。
- `Close`、`Shutdown`、`Disconnect`、`Sync` 等资源释放错误必须处理并记录。
- 日志不得输出密码、Token、私钥、SMTP 授权码或完整敏感连接串。
- 同一错误避免在相邻层级无意义重复打印；日志层级和错误返回责任必须清晰。

## 4. 测试代码

- 所有新增或修改的 `_test.go` 测试函数必须有注释，说明验证目标和关键场景。
- 测试辅助类型、Fake、Stub、Mock 及其方法必须有注释，说明模拟的行为。
- 测试代码同样必须处理错误和释放资源，不得因为“只是测试”降低代码质量。
- 单元测试默认不得依赖真实网络服务、固定长时间 `Sleep` 或无限阻塞。
- 异步测试使用 Channel、Context 和有上限的超时，确保失败时能够及时退出。
- 表驱动测试的 case 名称必须能够说明失败场景。

## 5. 外部服务与并发

- 外部服务调用必须支持 Context、超时和取消。
- 创建的连接、Listener、文件、Ticker、goroutine 和客户端必须明确关闭责任。
- goroutine 内部错误必须能够返回、记录或触发受控退出，禁止静默失败。
- Channel 的创建方、发送方、接收方和关闭方必须清晰，避免重复关闭、永久阻塞和 goroutine 泄漏。
- 重试必须有最大次数、退避策略和不可重试错误判断。

## 6. 修改完成后的检查

- 执行 `gofmt` 并确认没有格式差异。
- 执行目标模块单元测试。
- 涉及并发时执行 `go test -race`。
- 执行 `go vet`。
- 在不会触发真实外部服务的前提下执行全包编译检查。
- 执行 `git diff --check`，同步更新 README、路线图或相关设计文档。
